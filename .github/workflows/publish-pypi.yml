name: Publish to PyPI

on:
  push:
    tags:
      - "v[0-9]+.[0-9]+.[0-9]+*"

permissions:
  contents: read

jobs:
  test:
    name: Run test suite
    uses: ./.github/workflows/test.yaml

  build:
    name: Validate release and build distributions
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Ensure tagged commit is on main
        run: |
          set -euo pipefail
          commit="$(git rev-parse 'HEAD^{commit}')"
          if ! git merge-base --is-ancestor "$commit" origin/main; then
            echo "Tag ${GITHUB_REF_NAME} points to ${commit}, which is not on main."
            echo "Merge to main first, then tag the commit on main."
            exit 1
          fi
          echo "Release gate passed: ${commit} is on main."

      - name: Set up Python
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
        with:
          python-version: "3.14"

      - name: Validate tag matches package version
        run: |
          set -euo pipefail
          pkg_version="$(python - <<'PY'
          import tomllib

          with open("pyproject.toml", "rb") as pyproject_file:
              pyproject = tomllib.load(pyproject_file)

          print(pyproject.get("project", {}).get("version", ""))
          PY
          )"
          if [ -z "$pkg_version" ]; then
            echo "Could not resolve package version from pyproject.toml"
            exit 1
          fi
          if [ "${GITHUB_REF_NAME}" != "v${pkg_version}" ]; then
            echo "Tag/version mismatch: tag=${GITHUB_REF_NAME} expected=v${pkg_version}"
            exit 1
          fi
          echo "Tag/version check passed: ${GITHUB_REF_NAME}"

      - name: Validate source module entrypoint version
        run: |
          set -euo pipefail
          python -m pip install --upgrade pip
          python -m pip install -e .
          pkg_version="$(python - <<'PY'
          import tomllib

          with open("pyproject.toml", "rb") as pyproject_file:
              pyproject = tomllib.load(pyproject_file)

          print(pyproject.get("project", {}).get("version", ""))
          PY
          )"
          module_version="$(PYTHONPATH=src python -m phart --version)"
          if [ "$module_version" != "$pkg_version" ]; then
            echo "Module entrypoint/version mismatch: module=$module_version package=$pkg_version"
            exit 1
          fi
          echo "Source module entrypoint check passed: $module_version"

      - name: Build sdist and wheel
        run: |
          python -m pip install --upgrade pip
          python -m pip install build twine
          python -m build
          twine check dist/*

      - name: Validate distribution metadata version
        run: |
          set -euo pipefail
          python - <<'PY'
          import tarfile
          import tomllib
          from email.parser import Parser
          from pathlib import Path
          from zipfile import ZipFile

          with open("pyproject.toml", "rb") as pyproject_file:
              expected = tomllib.load(pyproject_file)["project"]["version"]

          versions = {}

          for wheel in Path("dist").glob("*.whl"):
              with ZipFile(wheel) as archive:
                  metadata_name = next(
                      name
                      for name in archive.namelist()
                      if name.endswith(".dist-info/METADATA")
                  )
                  metadata = Parser().parsestr(
                      archive.read(metadata_name).decode("utf-8")
                  )
              versions[wheel.name] = metadata["Version"]

          for sdist in Path("dist").glob("*.tar.gz"):
              with tarfile.open(sdist, "r:gz") as archive:
                  pkg_info_name = next(
                      name
                      for name in archive.getnames()
                      if name.endswith("/PKG-INFO")
                  )
                  pkg_info = archive.extractfile(pkg_info_name)
                  if pkg_info is None:
                      raise RuntimeError(f"Could not read {pkg_info_name}")
                  metadata = Parser().parsestr(pkg_info.read().decode("utf-8"))
              versions[sdist.name] = metadata["Version"]

          if not versions:
              raise RuntimeError("No distributions found in dist/")

          mismatches = {
              name: version
              for name, version in versions.items()
              if version != expected
          }
          if mismatches:
              details = ", ".join(
                  f"{name}={version}" for name, version in sorted(mismatches.items())
              )
              raise SystemExit(
                  f"Distribution metadata/version mismatch: expected={expected}; {details}"
              )

          print(f"Distribution metadata check passed: {expected}")
          PY

      - name: Upload dist artifacts
        uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4 # v5.0.0
        with:
          name: dist
          path: dist/

  publish:
    name: Publish to PyPI (Trusted Publishing)
    needs: [test, build]
    runs-on: ubuntu-latest
    environment:
      name: pypi
      url: https://pypi.org/p/phart
    permissions:
      id-token: write

    steps:
      - name: Download dist artifacts
        uses: actions/download-artifact@634f93cb2916e3fdff6788551b99b062d0335ce0 # v5.0.0
        with:
          name: dist
          path: dist/

      - name: Publish package to PyPI
        uses: pypa/gh-action-pypi-publish@dc37677b2e1c63e2034f94d8a5b11f265b73ba33 # v1.14.2
        with:
          attestations: true
